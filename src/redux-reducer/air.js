const initialState = {
  air: null,
};

const airReducer = (state = initialState, action) => {
  switch (action.type) {
    case 10:
      break;
    default:
      return state;
  }
};

export default airReducer;
